**SPQR Servus Publicus Help**
```
!help                   outputs this message

!roles                  outputs spqr roles info
!reqs                   outputs gladiator requirements
!rules                  outputs gladiator rules
!invite                 outputs invite message

!spqr                   outputs spqr translation
!loremipsum             outputs lorem ipsum
!suntzu                 outputs random sun tzu quote
!echo <in>              outputs the input

!serverinfo             outputs server info
!maplist                outputs current maplist
!playerlist             outputs current players

!getstats               outputs stats for current discord user
!getrank                outputs rank for current discord user
!register <steamid64>   registers current discord user with given steamid64

### Praefectus+
!resetsnd               resets snd on server
!rotatemap              rotates map on server
!setrandommap           sets random snd map on server
!setmap <mapugc> <mode> sets specific map and gamemode on server
!kick <steamid64>       kick player with given steamid64
!ban <steamid64>        ban player with given steamid64
!unban <steamid64>      unban player with given steamid64
!modlist                outputs list of server admins
!blacklist              outputs list of banned players
!pings                  outputs average ping for all current players

### Senate+
!writeas <chn> <in>     outputs the input to a given channel
!unregister <dscrdid>   unregisters given discordid
!genteams <msgid>       generates teams for given discord msg id
```
